- for item in @menu_item_data
  %div
    %input{:type=>"checkbox", :name=>"result_checkboxes", :id => item['id'], :checked=>false, :value => item['id'], :name => item['name'], :price => item['price']}= item['name'] + " " + item['price'].to_s
  -#    = check_box_tag item[:id], item[:id], false
  -#    = item[:name] + " " + item[:price].to_s
  
    
